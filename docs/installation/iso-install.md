---
title: ISO install
---

# ISO install

> **Stub.** Detailed walkthrough lands in v0.4.0.

This page will cover:

- Downloading the latest release ISO from the GitHub Releases page + verifying SHA256
- Flashing to a USB stick on macOS, Linux, and Windows (`dd`, Etcher, Rufus)
- Boot order setup on common server hardware (R720 iDRAC virtual media, NX-3200 BIOS)
- The interactive disk selection (intentionally manual to avoid accidentally erasing a drive-under-test)
- The unattended portion (Debian 12 install, package install, daemon bootstrap)
- First-boot setup wizard walkthrough with screenshots
- Post-install: where the dashboard lives, default credentials, what to do next

For now, see [`INSTALL.md`](https://github.com/JT4862/driveforge/blob/main/INSTALL.md) Path A.
